# @elephant-xyz/mcp configuration for duval, generated by the publish that produced these artifacts.
# publish: 2026-09-08T13:01:03.269Z (published)
#
# The PER-PUBLISH lines change on every publish. They are immutable CID URLs because the
# server hands the query table straight to DuckDB, which pins the ETag it first saw and fails
# every data tool the moment a mutable /ipns/ URL is re-pointed underneath a warm instance.
# The pipeline workflow applies them to the MCP deployment itself (`publish:apply-mcp-env`), and
# superseded CIDs stay served for PUBLISH_RETENTION_DAYS, so a missed apply degrades to stale
# data, never to a 504. Paste by hand only when that step is not configured.

# --- per publish: re-apply these two, then redeploy ---
# DuckDB reads this URL directly and pins its ETag for the life of a warm instance; a mutable /ipns/ URL breaks every data tool the moment the name moves.
PROPERTY_QUERY_TABLE_MAP={"duval":"https://ipfs.filebase.io/ipfs/bafybeibsrsm2jxnz2rljzvnjdrarydkdpj256tr3h6plohqefypjdbeguq"}
# Read with plain fetch, so a name would work, but pinning it to this publish keeps the reported coverage describing the exact parquet PROPERTY_QUERY_TABLE_MAP serves.
DATASET_COVERAGE_MAP={"duval":"https://ipfs.filebase.io/ipfs/bafybeienn5x4kwiq62bdqfrrj3ct6n5purotqiwrmxojxvbslh7s7tasju"}

# --- set once: stable across publishes ---
PROPERTY_QUERY_TABLE_DEFAULT_COUNTY=duval
ORACLE_OPEN_DATA_IPNS_MAP={"duval":"k51qzi5uqu5dig8412edx2zx5x4bxcmilhho2gusgvm7z0wsikrv13nxfzihm2"}
ORACLE_OPEN_DATA_DEFAULT_COUNTY=duval
PUBLISHED_COUNTY_CATALOG_URL=https://ipfs.filebase.io/ipns/k51qzi5uqu5dhbku9cn93fphs8tj703ydh5ypo2tk3k13d5t7tz08pr0qijy7d
